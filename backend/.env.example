PORT=3000

# TimescaleDB logging — ปิดไว้เป็นค่าเริ่มต้นสำหรับ MVP (ไม่ต้องรัน DB)
# ตั้งเป็น true เมื่อพร้อมเก็บ log ลง TimescaleDB
ENABLE_DB_LOGGING=false
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartflow
DB_USER=smartflow
DB_PASSWORD=changeme
