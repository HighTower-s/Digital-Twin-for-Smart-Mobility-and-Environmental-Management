PORT=3000

# The Prototype backend has no database — DB_* vars below are for MVP only,
# not used by the current spawn-event + in-memory counters code path.
# ENABLE_DB_LOGGING=false
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=smartflow
# DB_USER=smartflow
# DB_PASSWORD=changeme
